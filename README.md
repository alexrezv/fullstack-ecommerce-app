Project for the course "Full Stack: Angular and Java Spring Boot E-commerce Website" on Udemy

To build:
/root > mvn clean package

To run:
/root/ecommerce-zip > mvn -P Docker docker:build docker:start
